/* tslint:disable */
/* eslint-disable */
/**
 * lockally control plane
 * The lockally control plane lets integrators and direct customers manage everything except actual mail data flow (which uses standard JMAP / IMAP / SMTP-submission against the data plane endpoints).  **Authentication.** All `/v1/_*` endpoints require a Bearer API key in the `Authorization` header. Keys are formatted `lk_live_<8-char-prefix>_<32-char-secret>`. Generate the first key for a tenant via `cmd/seed`; subsequent keys via `POST /v1/api-keys`.  **Errors.** Failures return [RFC 9457](https://www.rfc-editor.org/rfc/rfc9457) `application/problem+json` documents.  **Scopes.** Each endpoint requires a specific scope on the presented key. Insufficient scope returns `403` with the required scope name in `detail`. 
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@lockally.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * 
 * @export
 * @interface VacationParams
 */
export interface VacationParams {
    /**
     * 
     * @type {string}
     * @memberof VacationParams
     */
    subject: string;
    /**
     * 
     * @type {string}
     * @memberof VacationParams
     */
    body: string;
    /**
     * 
     * @type {Date}
     * @memberof VacationParams
     */
    startsAt?: Date;
    /**
     * 
     * @type {Date}
     * @memberof VacationParams
     */
    endsAt?: Date;
}

/**
 * Check if a given object implements the VacationParams interface.
 */
export function instanceOfVacationParams(value: object): value is VacationParams {
    if (!('subject' in value) || value['subject'] === undefined) return false;
    if (!('body' in value) || value['body'] === undefined) return false;
    return true;
}

export function VacationParamsFromJSON(json: any): VacationParams {
    return VacationParamsFromJSONTyped(json, false);
}

export function VacationParamsFromJSONTyped(json: any, ignoreDiscriminator: boolean): VacationParams {
    if (json == null) {
        return json;
    }
    return {
        
        'subject': json['subject'],
        'body': json['body'],
        'startsAt': json['starts_at'] == null ? undefined : (new Date(json['starts_at'])),
        'endsAt': json['ends_at'] == null ? undefined : (new Date(json['ends_at'])),
    };
}

export function VacationParamsToJSON(json: any): VacationParams {
    return VacationParamsToJSONTyped(json, false);
}

export function VacationParamsToJSONTyped(value?: VacationParams | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'subject': value['subject'],
        'body': value['body'],
        'starts_at': value['startsAt'] == null ? value['startsAt'] : value['startsAt'].toISOString(),
        'ends_at': value['endsAt'] == null ? value['endsAt'] : value['endsAt'].toISOString(),
    };
}

