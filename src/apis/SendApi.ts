/* tslint:disable */
/* eslint-disable */
/**
 * lockally control plane
 * The lockally control plane lets integrators and direct customers manage everything except actual mail data flow (which uses standard JMAP / IMAP / SMTP-submission against the data plane endpoints).  **Authentication.** All `/v1/_*` endpoints require a Bearer API key in the `Authorization` header. Keys are formatted `lk_live_<8-char-prefix>_<32-char-secret>`. Generate the first key for a tenant via `cmd/seed`; subsequent keys via `POST /v1/api-keys`.  **Errors.** Failures return [RFC 9457](https://www.rfc-editor.org/rfc/rfc9457) `application/problem+json` documents.  **Scopes.** Each endpoint requires a specific scope on the presented key. Insufficient scope returns `403` with the required scope name in `detail`. 
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@lockally.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type Message,
    MessageFromJSON,
    MessageToJSON,
} from '../models/Message';
import {
    type MessageStats,
    MessageStatsFromJSON,
    MessageStatsToJSON,
} from '../models/MessageStats';
import {
    type Problem,
    ProblemFromJSON,
    ProblemToJSON,
} from '../models/Problem';
import {
    type V1MessagesGet200Response,
    V1MessagesGet200ResponseFromJSON,
    V1MessagesGet200ResponseToJSON,
} from '../models/V1MessagesGet200Response';
import {
    type V1SendBatchPost200Response,
    V1SendBatchPost200ResponseFromJSON,
    V1SendBatchPost200ResponseToJSON,
} from '../models/V1SendBatchPost200Response';
import {
    type V1SendBatchPostRequest,
    V1SendBatchPostRequestFromJSON,
    V1SendBatchPostRequestToJSON,
} from '../models/V1SendBatchPostRequest';
import {
    type V1SendPost202Response,
    V1SendPost202ResponseFromJSON,
    V1SendPost202ResponseToJSON,
} from '../models/V1SendPost202Response';
import {
    type V1SendPostRequest,
    V1SendPostRequestFromJSON,
    V1SendPostRequestToJSON,
} from '../models/V1SendPostRequest';

export interface V1MessagesGetRequest {
    status?: V1MessagesGetStatusEnum;
    sender?: string;
    q?: string;
    since?: Date;
    cursor?: string;
    limit?: number;
}

export interface V1MessagesIdDeleteRequest {
    id: string;
}

export interface V1MessagesIdGetRequest {
    id: string;
}

export interface V1MessagesStatsGetRequest {
    from?: Date;
    to?: Date;
    domain?: string;
}

export interface V1SendBatchPostOperationRequest {
    idempotencyKey: string;
    v1SendBatchPostRequest: V1SendBatchPostRequest;
}

export interface V1SendPostOperationRequest {
    idempotencyKey: string;
    v1SendPostRequest: V1SendPostRequest;
}

/**
 * SendApi - interface
 * 
 * @export
 * @interface SendApiInterface
 */
export interface SendApiInterface {
    /**
     * Creates request options for v1MessagesGet without sending the request
     * @param {'queued' | 'sending' | 'delivered' | 'bounced' | 'deferred' | 'complaint'} [status] 
     * @param {string} [sender] Exact match against the &#x60;from&#x60; mailbox.
     * @param {string} [q] Free-text search across subject + sender.
     * @param {Date} [since] Only messages queued at or after this RFC 3339 instant.
     * @param {string} [cursor] queued_at of the prior page boundary. Pass back the &#x60;next_cursor&#x60; returned by the previous call.
     * @param {number} [limit] 
     * @throws {RequiredError}
     * @memberof SendApiInterface
     */
    v1MessagesGetRequestOpts(requestParameters: V1MessagesGetRequest): Promise<runtime.RequestOpts>;

    /**
     * Returns recent outbound messages for the calling tenant, sorted newest first. Backs the send-status pill in the SvelteKit /sends view and the outbound search box. 
     * @summary List outbound messages
     * @param {'queued' | 'sending' | 'delivered' | 'bounced' | 'deferred' | 'complaint'} [status] 
     * @param {string} [sender] Exact match against the &#x60;from&#x60; mailbox.
     * @param {string} [q] Free-text search across subject + sender.
     * @param {Date} [since] Only messages queued at or after this RFC 3339 instant.
     * @param {string} [cursor] queued_at of the prior page boundary. Pass back the &#x60;next_cursor&#x60; returned by the previous call.
     * @param {number} [limit] 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SendApiInterface
     */
    v1MessagesGetRaw(requestParameters: V1MessagesGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<V1MessagesGet200Response>>;

    /**
     * Returns recent outbound messages for the calling tenant, sorted newest first. Backs the send-status pill in the SvelteKit /sends view and the outbound search box. 
     * List outbound messages
     */
    v1MessagesGet(requestParameters: V1MessagesGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<V1MessagesGet200Response>;

    /**
     * Creates request options for v1MessagesIdDelete without sending the request
     * @param {string} id 
     * @throws {RequiredError}
     * @memberof SendApiInterface
     */
    v1MessagesIdDeleteRequestOpts(requestParameters: V1MessagesIdDeleteRequest): Promise<runtime.RequestOpts>;

    /**
     * Cancels a still-scheduled message (future queued_at). Already sending/sent → 409.
     * @summary Cancel a scheduled send
     * @param {string} id 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SendApiInterface
     */
    v1MessagesIdDeleteRaw(requestParameters: V1MessagesIdDeleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>>;

    /**
     * Cancels a still-scheduled message (future queued_at). Already sending/sent → 409.
     * Cancel a scheduled send
     */
    v1MessagesIdDelete(requestParameters: V1MessagesIdDeleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void>;

    /**
     * Creates request options for v1MessagesIdGet without sending the request
     * @param {string} id 
     * @throws {RequiredError}
     * @memberof SendApiInterface
     */
    v1MessagesIdGetRequestOpts(requestParameters: V1MessagesIdGetRequest): Promise<runtime.RequestOpts>;

    /**
     * 
     * @summary Get message status
     * @param {string} id 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SendApiInterface
     */
    v1MessagesIdGetRaw(requestParameters: V1MessagesIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Message>>;

    /**
     * Get message status
     */
    v1MessagesIdGet(requestParameters: V1MessagesIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Message>;

    /**
     * Creates request options for v1MessagesStatsGet without sending the request
     * @param {Date} [from] Window start (default 7 days ago).
     * @param {Date} [to] Window end (default now).
     * @param {string} [domain] Filter by sender domain.
     * @throws {RequiredError}
     * @memberof SendApiInterface
     */
    v1MessagesStatsGetRequestOpts(requestParameters: V1MessagesStatsGetRequest): Promise<runtime.RequestOpts>;

    /**
     * Counts by delivery outcome (delivered/bounced/deferred/complaint) plus rates over a window, from the delivery-event store. Privacy-first: this reflects what receiving servers reported, NOT whether a human opened the mail — Lockally does no open/click tracking. 
     * @summary Aggregate delivery stats
     * @param {Date} [from] Window start (default 7 days ago).
     * @param {Date} [to] Window end (default now).
     * @param {string} [domain] Filter by sender domain.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SendApiInterface
     */
    v1MessagesStatsGetRaw(requestParameters: V1MessagesStatsGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MessageStats>>;

    /**
     * Counts by delivery outcome (delivered/bounced/deferred/complaint) plus rates over a window, from the delivery-event store. Privacy-first: this reflects what receiving servers reported, NOT whether a human opened the mail — Lockally does no open/click tracking. 
     * Aggregate delivery stats
     */
    v1MessagesStatsGet(requestParameters: V1MessagesStatsGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MessageStats>;

    /**
     * Creates request options for v1SendBatchPost without sending the request
     * @param {string} idempotencyKey 
     * @param {V1SendBatchPostRequest} v1SendBatchPostRequest 
     * @throws {RequiredError}
     * @memberof SendApiInterface
     */
    v1SendBatchPostRequestOpts(requestParameters: V1SendBatchPostOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Sends up to 500 messages in one call. Each is validated and enqueued independently — a bad message fails only its own slot (partial success, HTTP 200). One `Idempotency-Key` header covers the batch; per-message keys are derived as `<key>:<index>`. 
     * @summary Send a batch of emails
     * @param {string} idempotencyKey 
     * @param {V1SendBatchPostRequest} v1SendBatchPostRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SendApiInterface
     */
    v1SendBatchPostRaw(requestParameters: V1SendBatchPostOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<V1SendBatchPost200Response>>;

    /**
     * Sends up to 500 messages in one call. Each is validated and enqueued independently — a bad message fails only its own slot (partial success, HTTP 200). One `Idempotency-Key` header covers the batch; per-message keys are derived as `<key>:<index>`. 
     * Send a batch of emails
     */
    v1SendBatchPost(requestParameters: V1SendBatchPostOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<V1SendBatchPost200Response>;

    /**
     * Creates request options for v1SendPost without sending the request
     * @param {string} idempotencyKey 
     * @param {V1SendPostRequest} v1SendPostRequest 
     * @throws {RequiredError}
     * @memberof SendApiInterface
     */
    v1SendPostRequestOpts(requestParameters: V1SendPostOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Submits an email for delivery via lockally. Returns 202 immediately once the message is accepted into lockally\'s queue; the actual SMTP submission to the recipient is async. Track delivery via `GET /v1/messages/{id}` or webhook subscriptions for `delivery.delivered` / `delivery.bounced` / `delivery.complaint`.  **Idempotency-Key required.** Per design L7 — any unique string per send, 24-hour dedupe window. Repeated calls with the same key return byte-exact the original response and do NOT create a duplicate message.  **Sender authorisation.** `from` must be a non-disabled mailbox owned by the calling tenant on a verified domain. Sending from aliases is not yet supported.  **Rate cap.** Per-tenant `rate_cap_per_min` (returned on `/v1/tenant`) is enforced — 429 with `Retry-After: 60` once tripped.  **Recipient warning.** Over 25 total recipients (To+Cc+Bcc) sets a `warning` field in the response — large fan-outs queue noticeably at scale. Hard cap is 100/send. 
     * @summary Send an email
     * @param {string} idempotencyKey 
     * @param {V1SendPostRequest} v1SendPostRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SendApiInterface
     */
    v1SendPostRaw(requestParameters: V1SendPostOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<V1SendPost202Response>>;

    /**
     * Submits an email for delivery via lockally. Returns 202 immediately once the message is accepted into lockally\'s queue; the actual SMTP submission to the recipient is async. Track delivery via `GET /v1/messages/{id}` or webhook subscriptions for `delivery.delivered` / `delivery.bounced` / `delivery.complaint`.  **Idempotency-Key required.** Per design L7 — any unique string per send, 24-hour dedupe window. Repeated calls with the same key return byte-exact the original response and do NOT create a duplicate message.  **Sender authorisation.** `from` must be a non-disabled mailbox owned by the calling tenant on a verified domain. Sending from aliases is not yet supported.  **Rate cap.** Per-tenant `rate_cap_per_min` (returned on `/v1/tenant`) is enforced — 429 with `Retry-After: 60` once tripped.  **Recipient warning.** Over 25 total recipients (To+Cc+Bcc) sets a `warning` field in the response — large fan-outs queue noticeably at scale. Hard cap is 100/send. 
     * Send an email
     */
    v1SendPost(requestParameters: V1SendPostOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<V1SendPost202Response>;

}

/**
 * 
 */
export class SendApi extends runtime.BaseAPI implements SendApiInterface {

    /**
     * Creates request options for v1MessagesGet without sending the request
     */
    async v1MessagesGetRequestOpts(requestParameters: V1MessagesGetRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        if (requestParameters['sender'] != null) {
            queryParameters['sender'] = requestParameters['sender'];
        }

        if (requestParameters['q'] != null) {
            queryParameters['q'] = requestParameters['q'];
        }

        if (requestParameters['since'] != null) {
            queryParameters['since'] = (requestParameters['since'] as any).toISOString();
        }

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/messages`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns recent outbound messages for the calling tenant, sorted newest first. Backs the send-status pill in the SvelteKit /sends view and the outbound search box. 
     * List outbound messages
     */
    async v1MessagesGetRaw(requestParameters: V1MessagesGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<V1MessagesGet200Response>> {
        const requestOptions = await this.v1MessagesGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1MessagesGet200ResponseFromJSON(jsonValue));
    }

    /**
     * Returns recent outbound messages for the calling tenant, sorted newest first. Backs the send-status pill in the SvelteKit /sends view and the outbound search box. 
     * List outbound messages
     */
    async v1MessagesGet(requestParameters: V1MessagesGetRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<V1MessagesGet200Response> {
        const response = await this.v1MessagesGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for v1MessagesIdDelete without sending the request
     */
    async v1MessagesIdDeleteRequestOpts(requestParameters: V1MessagesIdDeleteRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling v1MessagesIdDelete().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/messages/{id}`;
        urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Cancels a still-scheduled message (future queued_at). Already sending/sent → 409.
     * Cancel a scheduled send
     */
    async v1MessagesIdDeleteRaw(requestParameters: V1MessagesIdDeleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.v1MessagesIdDeleteRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Cancels a still-scheduled message (future queued_at). Already sending/sent → 409.
     * Cancel a scheduled send
     */
    async v1MessagesIdDelete(requestParameters: V1MessagesIdDeleteRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.v1MessagesIdDeleteRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for v1MessagesIdGet without sending the request
     */
    async v1MessagesIdGetRequestOpts(requestParameters: V1MessagesIdGetRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling v1MessagesIdGet().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/messages/{id}`;
        urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Get message status
     */
    async v1MessagesIdGetRaw(requestParameters: V1MessagesIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Message>> {
        const requestOptions = await this.v1MessagesIdGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MessageFromJSON(jsonValue));
    }

    /**
     * Get message status
     */
    async v1MessagesIdGet(requestParameters: V1MessagesIdGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Message> {
        const response = await this.v1MessagesIdGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for v1MessagesStatsGet without sending the request
     */
    async v1MessagesStatsGetRequestOpts(requestParameters: V1MessagesStatsGetRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['from'] != null) {
            queryParameters['from'] = (requestParameters['from'] as any).toISOString();
        }

        if (requestParameters['to'] != null) {
            queryParameters['to'] = (requestParameters['to'] as any).toISOString();
        }

        if (requestParameters['domain'] != null) {
            queryParameters['domain'] = requestParameters['domain'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/messages/stats`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Counts by delivery outcome (delivered/bounced/deferred/complaint) plus rates over a window, from the delivery-event store. Privacy-first: this reflects what receiving servers reported, NOT whether a human opened the mail — Lockally does no open/click tracking. 
     * Aggregate delivery stats
     */
    async v1MessagesStatsGetRaw(requestParameters: V1MessagesStatsGetRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MessageStats>> {
        const requestOptions = await this.v1MessagesStatsGetRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MessageStatsFromJSON(jsonValue));
    }

    /**
     * Counts by delivery outcome (delivered/bounced/deferred/complaint) plus rates over a window, from the delivery-event store. Privacy-first: this reflects what receiving servers reported, NOT whether a human opened the mail — Lockally does no open/click tracking. 
     * Aggregate delivery stats
     */
    async v1MessagesStatsGet(requestParameters: V1MessagesStatsGetRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MessageStats> {
        const response = await this.v1MessagesStatsGetRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for v1SendBatchPost without sending the request
     */
    async v1SendBatchPostRequestOpts(requestParameters: V1SendBatchPostOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling v1SendBatchPost().'
            );
        }

        if (requestParameters['v1SendBatchPostRequest'] == null) {
            throw new runtime.RequiredError(
                'v1SendBatchPostRequest',
                'Required parameter "v1SendBatchPostRequest" was null or undefined when calling v1SendBatchPost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/send/batch`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: V1SendBatchPostRequestToJSON(requestParameters['v1SendBatchPostRequest']),
        };
    }

    /**
     * Sends up to 500 messages in one call. Each is validated and enqueued independently — a bad message fails only its own slot (partial success, HTTP 200). One `Idempotency-Key` header covers the batch; per-message keys are derived as `<key>:<index>`. 
     * Send a batch of emails
     */
    async v1SendBatchPostRaw(requestParameters: V1SendBatchPostOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<V1SendBatchPost200Response>> {
        const requestOptions = await this.v1SendBatchPostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1SendBatchPost200ResponseFromJSON(jsonValue));
    }

    /**
     * Sends up to 500 messages in one call. Each is validated and enqueued independently — a bad message fails only its own slot (partial success, HTTP 200). One `Idempotency-Key` header covers the batch; per-message keys are derived as `<key>:<index>`. 
     * Send a batch of emails
     */
    async v1SendBatchPost(requestParameters: V1SendBatchPostOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<V1SendBatchPost200Response> {
        const response = await this.v1SendBatchPostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for v1SendPost without sending the request
     */
    async v1SendPostRequestOpts(requestParameters: V1SendPostOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling v1SendPost().'
            );
        }

        if (requestParameters['v1SendPostRequest'] == null) {
            throw new runtime.RequiredError(
                'v1SendPostRequest',
                'Required parameter "v1SendPostRequest" was null or undefined when calling v1SendPost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/send`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: V1SendPostRequestToJSON(requestParameters['v1SendPostRequest']),
        };
    }

    /**
     * Submits an email for delivery via lockally. Returns 202 immediately once the message is accepted into lockally\'s queue; the actual SMTP submission to the recipient is async. Track delivery via `GET /v1/messages/{id}` or webhook subscriptions for `delivery.delivered` / `delivery.bounced` / `delivery.complaint`.  **Idempotency-Key required.** Per design L7 — any unique string per send, 24-hour dedupe window. Repeated calls with the same key return byte-exact the original response and do NOT create a duplicate message.  **Sender authorisation.** `from` must be a non-disabled mailbox owned by the calling tenant on a verified domain. Sending from aliases is not yet supported.  **Rate cap.** Per-tenant `rate_cap_per_min` (returned on `/v1/tenant`) is enforced — 429 with `Retry-After: 60` once tripped.  **Recipient warning.** Over 25 total recipients (To+Cc+Bcc) sets a `warning` field in the response — large fan-outs queue noticeably at scale. Hard cap is 100/send. 
     * Send an email
     */
    async v1SendPostRaw(requestParameters: V1SendPostOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<V1SendPost202Response>> {
        const requestOptions = await this.v1SendPostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1SendPost202ResponseFromJSON(jsonValue));
    }

    /**
     * Submits an email for delivery via lockally. Returns 202 immediately once the message is accepted into lockally\'s queue; the actual SMTP submission to the recipient is async. Track delivery via `GET /v1/messages/{id}` or webhook subscriptions for `delivery.delivered` / `delivery.bounced` / `delivery.complaint`.  **Idempotency-Key required.** Per design L7 — any unique string per send, 24-hour dedupe window. Repeated calls with the same key return byte-exact the original response and do NOT create a duplicate message.  **Sender authorisation.** `from` must be a non-disabled mailbox owned by the calling tenant on a verified domain. Sending from aliases is not yet supported.  **Rate cap.** Per-tenant `rate_cap_per_min` (returned on `/v1/tenant`) is enforced — 429 with `Retry-After: 60` once tripped.  **Recipient warning.** Over 25 total recipients (To+Cc+Bcc) sets a `warning` field in the response — large fan-outs queue noticeably at scale. Hard cap is 100/send. 
     * Send an email
     */
    async v1SendPost(requestParameters: V1SendPostOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<V1SendPost202Response> {
        const response = await this.v1SendPostRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
 * @export
 */
export const V1MessagesGetStatusEnum = {
    Queued: 'queued',
    Sending: 'sending',
    Delivered: 'delivered',
    Bounced: 'bounced',
    Deferred: 'deferred',
    Complaint: 'complaint'
} as const;
export type V1MessagesGetStatusEnum = typeof V1MessagesGetStatusEnum[keyof typeof V1MessagesGetStatusEnum];
