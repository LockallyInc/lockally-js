/* tslint:disable */
/* eslint-disable */
/**
 * lockally control plane
 * The lockally control plane lets integrators and direct customers manage everything except actual mail data flow (which uses standard JMAP / IMAP / SMTP-submission against the data plane endpoints).  **Authentication.** All `/v1/_*` endpoints require a Bearer API key in the `Authorization` header. Keys are formatted `lk_live_<8-char-prefix>_<32-char-secret>`. Generate the first key for a tenant via `cmd/seed`; subsequent keys via `POST /v1/api-keys`.  **Errors.** Failures return [RFC 9457](https://www.rfc-editor.org/rfc/rfc9457) `application/problem+json` documents.  **Scopes.** Each endpoint requires a specific scope on the presented key. Insufficient scope returns `403` with the required scope name in `detail`. 
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@lockally.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * 
 * @export
 * @interface MessageStatsWindow
 */
export interface MessageStatsWindow {
    /**
     * 
     * @type {Date}
     * @memberof MessageStatsWindow
     */
    from?: Date;
    /**
     * 
     * @type {Date}
     * @memberof MessageStatsWindow
     */
    to?: Date;
}

/**
 * Check if a given object implements the MessageStatsWindow interface.
 */
export function instanceOfMessageStatsWindow(value: object): value is MessageStatsWindow {
    return true;
}

export function MessageStatsWindowFromJSON(json: any): MessageStatsWindow {
    return MessageStatsWindowFromJSONTyped(json, false);
}

export function MessageStatsWindowFromJSONTyped(json: any, ignoreDiscriminator: boolean): MessageStatsWindow {
    if (json == null) {
        return json;
    }
    return {
        
        'from': json['from'] == null ? undefined : (new Date(json['from'])),
        'to': json['to'] == null ? undefined : (new Date(json['to'])),
    };
}

export function MessageStatsWindowToJSON(json: any): MessageStatsWindow {
    return MessageStatsWindowToJSONTyped(json, false);
}

export function MessageStatsWindowToJSONTyped(value?: MessageStatsWindow | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'from': value['from'] == null ? value['from'] : value['from'].toISOString(),
        'to': value['to'] == null ? value['to'] : value['to'].toISOString(),
    };
}

