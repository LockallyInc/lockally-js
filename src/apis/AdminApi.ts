/* tslint:disable */
/* eslint-disable */
/**
 * lockally control plane
 * The lockally control plane lets integrators and direct customers manage everything except actual mail data flow (which uses standard JMAP / IMAP / SMTP-submission against the data plane endpoints).  **Authentication.** All `/v1/_*` endpoints require a Bearer API key in the `Authorization` header. Keys are formatted `lk_live_<8-char-prefix>_<32-char-secret>`. Generate the first key for a tenant via `cmd/seed`; subsequent keys via `POST /v1/api-keys`.  **Errors.** Failures return [RFC 9457](https://www.rfc-editor.org/rfc/rfc9457) `application/problem+json` documents.  **Scopes.** Each endpoint requires a specific scope on the presented key. Insufficient scope returns `403` with the required scope name in `detail`. 
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@lockally.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type Problem,
    ProblemFromJSON,
    ProblemToJSON,
} from '../models/Problem';
import {
    type V1AdminLoginPost200Response,
    V1AdminLoginPost200ResponseFromJSON,
    V1AdminLoginPost200ResponseToJSON,
} from '../models/V1AdminLoginPost200Response';
import {
    type V1AdminLoginPostRequest,
    V1AdminLoginPostRequestFromJSON,
    V1AdminLoginPostRequestToJSON,
} from '../models/V1AdminLoginPostRequest';
import {
    type V1AdminMeGet200Response,
    V1AdminMeGet200ResponseFromJSON,
    V1AdminMeGet200ResponseToJSON,
} from '../models/V1AdminMeGet200Response';

export interface V1AdminLoginPostOperationRequest {
    v1AdminLoginPostRequest: V1AdminLoginPostRequest;
}

/**
 * AdminApi - interface
 * 
 * @export
 * @interface AdminApiInterface
 */
export interface AdminApiInterface {
    /**
     * Creates request options for v1AdminLoginPost without sending the request
     * @param {V1AdminLoginPostRequest} v1AdminLoginPostRequest 
     * @throws {RequiredError}
     * @memberof AdminApiInterface
     */
    v1AdminLoginPostRequestOpts(requestParameters: V1AdminLoginPostOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Exchanges an admin\'s email + password for a session token. The web console at `app.lockally.com` posts this on form submission and stores the returned token in an httpOnly cookie.  **No enumeration leak.** Wrong-email and wrong-password both return the same 401 with title \"Invalid credentials\". The argon2id verify runs even on lookup miss (well, structurally — the lookup fails fast but the response shape is constant) so timing leaks are bounded.  Tokens are prefixed `adm_sess_` and valid for 7 days. Use as the `Authorization: Bearer` value on all subsequent calls. 
     * @summary Tenant-admin email+password login
     * @param {V1AdminLoginPostRequest} v1AdminLoginPostRequest 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApiInterface
     */
    v1AdminLoginPostRaw(requestParameters: V1AdminLoginPostOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<V1AdminLoginPost200Response>>;

    /**
     * Exchanges an admin\'s email + password for a session token. The web console at `app.lockally.com` posts this on form submission and stores the returned token in an httpOnly cookie.  **No enumeration leak.** Wrong-email and wrong-password both return the same 401 with title \"Invalid credentials\". The argon2id verify runs even on lookup miss (well, structurally — the lookup fails fast but the response shape is constant) so timing leaks are bounded.  Tokens are prefixed `adm_sess_` and valid for 7 days. Use as the `Authorization: Bearer` value on all subsequent calls. 
     * Tenant-admin email+password login
     */
    v1AdminLoginPost(requestParameters: V1AdminLoginPostOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<V1AdminLoginPost200Response>;

    /**
     * Creates request options for v1AdminLogoutPost without sending the request
     * @throws {RequiredError}
     * @memberof AdminApiInterface
     */
    v1AdminLogoutPostRequestOpts(): Promise<runtime.RequestOpts>;

    /**
     * Deletes the session row from the database. Idempotent — calling logout on an already-invalid token returns 204 anyway. 
     * @summary Invalidate the current admin session
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApiInterface
     */
    v1AdminLogoutPostRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>>;

    /**
     * Deletes the session row from the database. Idempotent — calling logout on an already-invalid token returns 204 anyway. 
     * Invalidate the current admin session
     */
    v1AdminLogoutPost(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void>;

    /**
     * Creates request options for v1AdminMeGet without sending the request
     * @throws {RequiredError}
     * @memberof AdminApiInterface
     */
    v1AdminMeGetRequestOpts(): Promise<runtime.RequestOpts>;

    /**
     * Returns the admin profile + tenant for the session token presented in `Authorization: Bearer`. Used by the web console\'s layout load function to populate the sidebar.  Returns 403 if called with an API key (lk_live_*) bearer — admin context only exists for session tokens. 
     * @summary Get the current admin + tenant
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApiInterface
     */
    v1AdminMeGetRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<V1AdminMeGet200Response>>;

    /**
     * Returns the admin profile + tenant for the session token presented in `Authorization: Bearer`. Used by the web console\'s layout load function to populate the sidebar.  Returns 403 if called with an API key (lk_live_*) bearer — admin context only exists for session tokens. 
     * Get the current admin + tenant
     */
    v1AdminMeGet(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<V1AdminMeGet200Response>;

}

/**
 * 
 */
export class AdminApi extends runtime.BaseAPI implements AdminApiInterface {

    /**
     * Creates request options for v1AdminLoginPost without sending the request
     */
    async v1AdminLoginPostRequestOpts(requestParameters: V1AdminLoginPostOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['v1AdminLoginPostRequest'] == null) {
            throw new runtime.RequiredError(
                'v1AdminLoginPostRequest',
                'Required parameter "v1AdminLoginPostRequest" was null or undefined when calling v1AdminLoginPost().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';


        let urlPath = `/v1/admin/login`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: V1AdminLoginPostRequestToJSON(requestParameters['v1AdminLoginPostRequest']),
        };
    }

    /**
     * Exchanges an admin\'s email + password for a session token. The web console at `app.lockally.com` posts this on form submission and stores the returned token in an httpOnly cookie.  **No enumeration leak.** Wrong-email and wrong-password both return the same 401 with title \"Invalid credentials\". The argon2id verify runs even on lookup miss (well, structurally — the lookup fails fast but the response shape is constant) so timing leaks are bounded.  Tokens are prefixed `adm_sess_` and valid for 7 days. Use as the `Authorization: Bearer` value on all subsequent calls. 
     * Tenant-admin email+password login
     */
    async v1AdminLoginPostRaw(requestParameters: V1AdminLoginPostOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<V1AdminLoginPost200Response>> {
        const requestOptions = await this.v1AdminLoginPostRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1AdminLoginPost200ResponseFromJSON(jsonValue));
    }

    /**
     * Exchanges an admin\'s email + password for a session token. The web console at `app.lockally.com` posts this on form submission and stores the returned token in an httpOnly cookie.  **No enumeration leak.** Wrong-email and wrong-password both return the same 401 with title \"Invalid credentials\". The argon2id verify runs even on lookup miss (well, structurally — the lookup fails fast but the response shape is constant) so timing leaks are bounded.  Tokens are prefixed `adm_sess_` and valid for 7 days. Use as the `Authorization: Bearer` value on all subsequent calls. 
     * Tenant-admin email+password login
     */
    async v1AdminLoginPost(requestParameters: V1AdminLoginPostOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<V1AdminLoginPost200Response> {
        const response = await this.v1AdminLoginPostRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for v1AdminLogoutPost without sending the request
     */
    async v1AdminLogoutPostRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/logout`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Deletes the session row from the database. Idempotent — calling logout on an already-invalid token returns 204 anyway. 
     * Invalidate the current admin session
     */
    async v1AdminLogoutPostRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.v1AdminLogoutPostRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Deletes the session row from the database. Idempotent — calling logout on an already-invalid token returns 204 anyway. 
     * Invalidate the current admin session
     */
    async v1AdminLogoutPost(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.v1AdminLogoutPostRaw(initOverrides);
    }

    /**
     * Creates request options for v1AdminMeGet without sending the request
     */
    async v1AdminMeGetRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/admin/me`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns the admin profile + tenant for the session token presented in `Authorization: Bearer`. Used by the web console\'s layout load function to populate the sidebar.  Returns 403 if called with an API key (lk_live_*) bearer — admin context only exists for session tokens. 
     * Get the current admin + tenant
     */
    async v1AdminMeGetRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<V1AdminMeGet200Response>> {
        const requestOptions = await this.v1AdminMeGetRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => V1AdminMeGet200ResponseFromJSON(jsonValue));
    }

    /**
     * Returns the admin profile + tenant for the session token presented in `Authorization: Bearer`. Used by the web console\'s layout load function to populate the sidebar.  Returns 403 if called with an API key (lk_live_*) bearer — admin context only exists for session tokens. 
     * Get the current admin + tenant
     */
    async v1AdminMeGet(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<V1AdminMeGet200Response> {
        const response = await this.v1AdminMeGetRaw(initOverrides);
        return await response.value();
    }

}
