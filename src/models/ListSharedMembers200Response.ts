/* tslint:disable */
/* eslint-disable */
/**
 * lockally control plane
 * The lockally control plane lets integrators and direct customers manage everything except actual mail data flow (which uses standard JMAP / IMAP / SMTP-submission against the data plane endpoints).  **Authentication.** All `/v1/_*` endpoints require a Bearer API key in the `Authorization` header. Keys are formatted `lk_live_<8-char-prefix>_<32-char-secret>`. Generate the first key for a tenant via `cmd/seed`; subsequent keys via `POST /v1/api-keys`.  **Errors.** Failures return [RFC 9457](https://www.rfc-editor.org/rfc/rfc9457) `application/problem+json` documents.  **Scopes.** Each endpoint requires a specific scope on the presented key. Insufficient scope returns `403` with the required scope name in `detail`. 
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@lockally.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { SharedMember } from './SharedMember';
import {
    SharedMemberFromJSON,
    SharedMemberFromJSONTyped,
    SharedMemberToJSON,
    SharedMemberToJSONTyped,
} from './SharedMember';

/**
 * 
 * @export
 * @interface ListSharedMembers200Response
 */
export interface ListSharedMembers200Response {
    /**
     * 
     * @type {Array<SharedMember>}
     * @memberof ListSharedMembers200Response
     */
    data: Array<SharedMember>;
}

/**
 * Check if a given object implements the ListSharedMembers200Response interface.
 */
export function instanceOfListSharedMembers200Response(value: object): value is ListSharedMembers200Response {
    if (!('data' in value) || value['data'] === undefined) return false;
    return true;
}

export function ListSharedMembers200ResponseFromJSON(json: any): ListSharedMembers200Response {
    return ListSharedMembers200ResponseFromJSONTyped(json, false);
}

export function ListSharedMembers200ResponseFromJSONTyped(json: any, ignoreDiscriminator: boolean): ListSharedMembers200Response {
    if (json == null) {
        return json;
    }
    return {
        
        'data': ((json['data'] as Array<any>).map(SharedMemberFromJSON)),
    };
}

export function ListSharedMembers200ResponseToJSON(json: any): ListSharedMembers200Response {
    return ListSharedMembers200ResponseToJSONTyped(json, false);
}

export function ListSharedMembers200ResponseToJSONTyped(value?: ListSharedMembers200Response | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'data': ((value['data'] as Array<any>).map(SharedMemberToJSON)),
    };
}

